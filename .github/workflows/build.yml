name: Build unsigned iOS .ipa from HTML

on:
  workflow_dispatch:
    inputs:
      app_name:
        description: "App name"
        required: true
        default: MyHTMLApp
      html_file:
        description: "HTML filename (index.html)"
        required: true
        default: index.html

jobs:
  build-ios:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Prepare Capacitor project
        run: |
          npm init -y
          npm install @capacitor/cli @capacitor/core @capacitor/ios
          mkdir -p www
          echo '<html><body><h1>Hello iOS</h1></body></html>' > www/index.html
          APP_NAME="${{ github.event.inputs.app_name }}"
          PACKAGE_ID="com.${{ github.actor }}.${{ github.event.inputs.app_name }}"
          npx cap init "$APP_NAME" "$PACKAGE_ID" --web-dir=www

      - name: Install CocoaPods
        run: sudo gem install cocoapods

      - name: Add iOS platform
        run: |
          npx cap add ios || true
          echo "✅ iOS folder created:"
          ls -R ios || true

      - name: Patch Podfile and Xcode project for iOS 14.0
        run: |
          cd ios/App
          # 1. Ensure Podfile exists and set platform
          if [ -f Podfile ]; then
            sed -i '' 's/platform :ios.*/platform :ios, "14.0"/' Podfile || true
          else
            echo 'platform :ios, "14.0"' > Podfile
            echo 'target "App" do' >> Podfile
            echo '  use_frameworks!' >> Podfile
            echo '  use_modular_headers!' >> Podfile
            echo 'end' >> Podfile
          fi

          # 2. Fix Xcode project deployment target
          if [ -f "../App.xcodeproj/project.pbxproj" ]; then
            sed -i '' 's/IPHONEOS_DEPLOYMENT_TARGET = [0-9.]*;/IPHONEOS_DEPLOYMENT_TARGET = 14.0;/' ../App.xcodeproj/project.pbxproj || true
          fi

          echo "✅ Deployment target set to iOS 14.0"
          cat Podfile | head -n 5

      - name: Install Pods
        run: |
          cd ios/App
          pod repo update
          pod install --verbose

      - name: Build .app (no signing)
        run: |
          cd ios/App
          npx cap sync ios || true
          xcodebuild \
            -workspace App.xcworkspace \
            -scheme App \
            -configuration Release \
            -sdk iphoneos \
            BUILD_DIR=$PWD/build \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            clean build

      - name: Package unsigned .ipa
        run: |
          cd ios/App
          mkdir -p Payload
          cp -R build/Release-iphoneos/App.app Payload/
          zip -r MyApp_unsigned.ipa Payload
          mkdir -p build/ipa
          mv MyApp_unsigned.ipa build/ipa/

      - name: Upload unsigned .ipa
        uses: actions/upload-artifact@v4
        with:
          name: ios-app
          path: ios/App/build/ipa/MyApp_unsigned.ipa
          if-no-files-found: error
          retention-days: 7
